

//to upload files to firestore
const uploadFiles = (attachments) => {
    try {
        
    } catch (error) {
        console.log("Error uploading files: ", error);
    }
}

module.exports = uploadFiles;